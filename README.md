# Iris

The application lives in `USB_ASSISTANT/`.

Start here:

- `USB_ASSISTANT/README.md`

Build a plug-and-play USB release:

```bash
./tools/build_usb_release.sh
```

Sync prebuilt binaries from GitHub CI:

```bash
./tools/sync_binaries_from_github.sh
```
